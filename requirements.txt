# Python >= 3.10. Install with: pip install -r requirements.txt
# Estimation and tables
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
honestdid>=0.1        # Honest DiD reference implementation (script 82)
# Figures (script 77; requires a LaTeX installation usable by matplotlib)
matplotlib>=3.7
SciencePlots>=2.1
# Data pipeline (pipeline/*.py; not needed to reproduce the tables from the shipped panels)
openpyxl>=3.1
networkx>=2.8
tqdm>=4.62
